<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lgg.upload.mapper.UserMapper">
    <select id="selectUser" parameterType="java.lang.String" resultType="lgg.upload.model.User">
          SELECT * FROM yonghui WHERE yonghuiming =#{uoo};
    </select>
</mapper>